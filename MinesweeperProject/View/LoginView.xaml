<UserControl x:Class="MinesweeperProject.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="지뢰찾기 로그인" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            <TextBox Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" 
                     Width="250" Height="35" Margin="0,0,0,10" VerticalContentAlignment="Center"/>
            <Button Content="게임 시작" Command="{Binding LoginCommand}" 
                    Width="250" Height="40" Background="#007ACC" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>